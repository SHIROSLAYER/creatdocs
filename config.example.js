/* =====================================================================
   Configuração LOCAL do Gerador de Documentos — Infobarra
   ---------------------------------------------------------------------
   Copie este arquivo para "config.local.js" e preencha com os dados reais.
   config.local.js NÃO vai para o git (dados pessoais ficam só na máquina).
   ===================================================================== */
window.APP_CONFIG = {
  /* Testemunhas que saem fixas no Contrato de Comodato (nome + CPF). */
  witnesses: [
    { name: '', cpf: '' },
    { name: '', cpf: '' },
  ],
};
